// Gerado automaticamente pelo "Gerar" do Studio (scripts/generator.mjs) — não edite à mão.
import type { GeneratedRoute } from "./routes.types";
import Home from "@/pages/Home";

export const routes: GeneratedRoute[] = [
  { path: "/home", name: "Home", component: Home },
];
